spring:
  application:
    name: mock-pg

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

mockpg:
  webhook:
    target-url: http://localhost:8080/api/webhooks/payments
    secret: groupdrop-mock-pg-secret
